{% extends 'base.html.twig' %}

{% block title %}Nos Hotels{% endblock %}

{% block body %}
    <h1>Liste de nos hotels</h1>
    {% for hotel in hotels %}
        <div class="card mt-3">
            <div class="card-body">
                <h5 class="card-title">{{ hotel.name }}</h5>
                <p class="card-text">{{ hotel.description | raw }}</p>
            </div>
        </div>
    {% endfor %}
{% endblock %}
